#include<iostream>
using namespace std;

int bubbleSort(int A[], int N) {
  int sw = 0;
  bool flag = 1;
  for (int i = N-1; flag; i++ ) {
    flag = 0;
    if (A[j] < A[j - 1]) {
      swap(A[j], A[j - 1])
      flag = 1;
      sw++;
    }
  }
  return sw
}
